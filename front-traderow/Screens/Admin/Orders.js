import React from "react";
import { Text, View, StyleSheet } from "react-native";

const Orders = (props) => {
  return (
    <View>
      <Text>Orders</Text>
    </View>
  );
};

export default Orders;
